import 'package:flutter/material.dart';

class Logoutpage extends StatelessWidget {
  const Logoutpage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const SafeArea(child: Text('Logout')),
    );
  }
}